@using DigiProj.Services.Interfaces;
@inject IUserApiService apiUserService

@{
	var apiMenu = await apiUserService.GetMenusAsync();
	var Menu = apiMenu.Data;
}

@*Permission CRUD*@
@if (Menu.First().Controller == "projects-card" && Menu.First().IsCreate == false)
{
	<style>
		#cr {
			display: none;
		}
	</style>
}
@if (Menu.First().Controller == "projects-card" && Menu.First().IsDelete == false)
{
	<style>
		#dlt {
			display: none;
		}
	</style>
}
@if (Menu.First().Controller == "projects-card" && Menu.First().IsRead == false)
{
	<style>
		#dt {
			display: none;
		}
	</style>
}
@if (Menu.First().Controller == "projects-card" && Menu.First().IsEdit == false)
{
	<style>
		#ed {
			display: none;
		}
	</style>
}
@if (Menu.First().Controller == "projects-card" && Menu.First().IsUpload == false)
{
	<style>
		#up {
			display: none;
		}
	</style>
}
@*End Permission CRUD*@